---

- name: Ensure cron is installed
  apt:
    pkg: cron
    state: present
  tags:
    - cron

- name: Ensure Cron is running
  service:
    name: cron
    state: started
    enabled: yes
  tags:
    - cronc

- include: jobs.yml
  tags:
    - cron

